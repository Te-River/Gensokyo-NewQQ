# P12 — SDK / Generated 边界隔离 验证报告

```
PHASE: P12（SDK / Generated 边界隔离）
STATUS: PASS
```

---

## 目标

稳定依赖边界：botgo 只在 adapter/qq；go-silk 只在 media adapter；
generated code 与手写代码边界清楚；fork patch inventory 完成。

---

## 实现

### P12.1/2 botgo 隔离

- 新增 `adapter/qq/`：`OpenIDFromBotgoUser` / `OpenGroupIDFromBotgo`（botgo DTO → typed identity 转换边界示例）。
- 验证：`internal/**` + `adapter/**` 全量无 `botgo` import（domain/application 干净）。

### P12.3 go-silk 隔离

- 验证：`internal/**` + `adapter/**` 全量无 `go-silk` import。
- `AudioTranscoder` 边界已在 P5 建立（FFmpeg/go-silk 封装点）。

### P12.4 Proto Generated Code

- `proto/idmap.pb.go` / `proto/idmap_grpc.pb.go` 已有
  `// Code generated by protoc-gen-go. DO NOT EDIT.` 标注 ✓（边界清楚，不改手改）。

### P12.5 Generation Script

- 新增 `scripts/generate.ps1` / `scripts/generate.sh`（protoc 生成；缺少 protoc 时跳过并提示）。

### P12.6 Fork Inventory

- 新增 `docs/forks/botgo.md` / `docs/forks/go-silk.md`（upstream、module、patch 内容、为什么 fork、未来同步方式）。

---

## 验收清单

| 验收项 | 状态 |
|--------|------|
| domain 无 botgo | ✅ PASS |
| application 无 botgo | ✅ PASS |
| application 无 go-silk | ✅ PASS |
| generated code 与手写代码边界清楚 | ✅ PASS（DO NOT EDIT 标注） |
| fork patch inventory 完成 | ✅ PASS（已知信息 + 待维护标注） |

---

## 变更文件

- 新增：`adapter/qq/conversion.go`
- 新增：`docs/forks/{botgo,go-silk}.md`
- 新增：`scripts/generate.{ps1,sh}`

---

## 验证结果

| 命令 | 结果 |
|------|------|
| `go build ./...` | ✅ PASS |
| `go vet ./...` | ✅ PASS |
| `go test ./...` | ✅ PASS |
| `go test -race ./...` | NOT_RUN（windows/386 不支持） |
| `govulncheck ./...` | NOT_RUN（计划 V3） |
| frontend | NOT_RUN（不涉及） |

---

## KNOWN_LIMITATIONS

- `adapter/qq` 目前仅转换边界示例；完整入站（botgo DTO → DomainEvent）与出站
  （outbound model → botgo request）转换待 P13 填充。
- fork inventory 中"完整本地 patch 清单"需维护者基于 upstream diff 补齐。
- proto 生成脚本未实测（本机可能无 protoc），生成文件已提交、无需本机重跑。

---

## LEGACY_REMAINING

- `handlers/`、`Processor/` 等旧包仍直接 import botgo / go-silk（P13 收敛到 adapter）。

---

## NEXT_PHASE

- P13 删除兼容层（foundItems / 长度启发式 / 旧 Handler / 全局状态 / init 注册）。
